# 프로젝트 명: 모바일 OCR 차량 번호판 인식 및 Google Keep 연동 웹앱 (세션 유지/메모 선택 지원)

## 1. 프로젝트 개요
사용자가 웹페이지 접속 시 구글 로그인 및 대상 Google Keep 메모를 선택하고, 이후 방문 시 해당 설정을 자동으로 유지하면서 카메라로 차량 번호판을 찍어 특정 메모의 체크리스트를 자동 업데이트하는 Streamlit 기반 웹 애플리케이션을 구축해 주세요.

---

## 2. 주요 서비스 이용 흐름 (User Flow)

1. **최초 접속 시 (설정 단계)**
   - 구글 계정 로그인 (구글 이메일 & 앱 비밀번호/마스터 토큰 입력).
   - 로그인 성공 시, 계정 내 구글 킵 메모 목록(본인 작성 및 공유받은 메모 포함)을 조회하여 `st.selectbox`로 보여줌.
   - 사용자가 작동 대상이 될 '구글 킵 메모'를 선택 후 [설정 완료] 버튼 클릭.
   - **설정 정보 저장**: 로그인 정보 및 선택한 메모 ID/제목을 브라우저 로컬 저장소(Streamlit Session State 또는 Query Params/Cookies)에 기억시킴.

2. **재접속 및 평시 사용 시 (카메라 단계)**
   - 이미 설정이 완료되어 있다면 바로 **"현재 선택된 킵 메모: [메모 제목]"** 표시와 함께 **카메라 촬영 화면(`st.camera_input`)**을 메인에 노출.
   - 상단 또는 사이드바에 **[다른 구글킵 메모 선택]** 및 **[로그아웃]** 버튼을 배치하여 언제든 대상 메모나 계정을 변경할 수 있게 함.

3. **촬영 및 번호판 인식/체크 단계**
   - 카메라로 촬영 후 OCR(Tesseract) 및 정규식 처리로 차량 번호판 추출.
   - 저장되어 있는 구글 킵 메모를 불러와 해당 차량 번호 항목의 체크박스(`item.checked = True`)를 체크하고 동기화(`keep.sync()`).

---

## 3. 기술적 세부 요구사항

### A. 구글 킵 연동 및 영속성 (gkeepapi)
- `gkeepapi` 라이브러리를 사용해 구글 킵 노트 목록 및 체크리스트를 다룸.
- 재방문 시 매번 로그인이나 메모 선택을 다시 하지 않도록 사용자 로그인 토큰 및 선택된 메모 ID를 세션/로컬에 유지 관리.
- 선택된 메모 내에서 차량 번호가 발견되면 체크 처리 후 성공 메시지(`st.success`) 및 축하 효과(`st.balloons()`) 출력.

### B. OCR 및 모바일 카메라 최적화
- `st.camera_input` 사용 (아이폰/안드로이드 카메라 직관적 연결).
- PyTesseract + OpenCV/Pillow 전처리(그레이스케일, 대비 강화)를 적용하여 한국어 번호판(`12가3456`, `123가3456` 등) 추출.
- 인식 실패 시 사용자가 번호를 수동으로 입력해 체크할 수 있는 예비 입력창(Manual Fallback) 제공.

---

## 4. 생성해야 하는 프로젝트 파일 구조

1. `app.py`: UI 흐름(로그인/메모선택 -> 카메라촬영 -> OCR 및 킵업데이트)이 통합된 Streamlit 메인 파이썬 코드
2. `requirements.txt`:
   ```txt
   streamlit
   gkeepapi
   pytesseract
   pillow
   opencv-python-headless
